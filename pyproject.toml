[project]
name = "openai-agents-api-modal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rafael", email = "rafaelpierre@github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "modal[api-proxy-support]>=1.3.4,<2",
    "openai>=3.14.0",
]

[project.scripts]
openai-agents-api-modal = "openai_agents_api_modal:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
